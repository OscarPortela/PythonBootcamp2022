#Exercise - Sum 2 digits by them index value, getting the result of the first number, plus the second number from a two digit input number.

two_digit_number = input("Type a two digit number: ")

val1 = two_digit_number[0]
val2 = two_digit_number[1]
result = int(val1) + int(val2)

print(result)
